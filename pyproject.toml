[project]
name = "rec-mcp"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.10.1",
    "python-dotenv>=1.1.1",
    "requests>=2.32.4",
]
